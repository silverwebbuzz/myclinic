.claude/settings.local.json
.claude/settings.json

# graphify generated knowledge graph
graphify-out/

# Large MySQL dumps — exceed GitHub's 100MB limit; keep local only.
# Real schema lives in app/database/migrations/ and document/seeds/.
document/silverwebbuzz_in_myclinic*.sql
*.sql.gz
*.dump
